
const SimpleCollectible = artifacts.require('./SimpleCollectible.sol') // get the smart contract

require('chai') // import the chai testing library
  .use(require('chai-as-promised'))
  .should()


contract('SimpleCollectible', (accounts) => {
  describe('deployment', async () => {
    it('deploys successfully', async () => {
      
    })
  })
} )
